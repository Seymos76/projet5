{# templates/AdminSpace/waitingCaptures.html.twig #}

{% extends "layout.html.twig" %}

{% block title %} Espace d'administration - Observations en attente de validation {% endblock %}
{% block stylesheets %} {% endblock %}
{% block body %} 
	<h1>Espace administration</h1>
	<h2>Observations en attente de validation</h2>

	<section>
		{{ include('AdminSpace/waitingCapturesModel.html.twig')}}
		<div>
			<ul>
				{% if nbWaitingForValidationCapturesPages > 1 %}
					<p> 
						<a href="{{ path('espaceAdministration') }}"> Retour </a> 
					</p>
					{% if previousPage > 1 %}
					<p> 
						<a href="{{ path('espaceAdminObservationsEnAttente', {page: previousPage}) }}"> << </a> 
					</p>
					{% endif %}
					{% for i in 2..nbWaitingForValidationCapturesPages %}
						<li {% if page == i %} class="active" {% endif %}>
   							<a href="{{ path('espaceAdminObservationsEnAttente', {page: i}) }}"> {{i}} </a>
   						</li>
   					{% endfor %}
    				<p>
    					{% if nextPage <= nbWaitingForValidationCapturesPages %}
    						<a href="{{ path('espaceAdminObservationsEnAttente', {page: nextPage}) }}"> >> </a>
    					{% endif %}
    				</p> 
    			{% endif %}
			</ul>
		</div>
	</section>
{% endblock %}
{% block javascripts %} {% endblock %}
