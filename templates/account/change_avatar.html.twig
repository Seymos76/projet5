{% extends "base.html.twig" %}

{% block body %}
    <img src="{{ asset('uploads/avatar/' ~ app.user.avatar) }}" alt="" height="150">
        {{ form_start(form) }}
        {{ form_errors(form) }}
        {{ form_widget(form) }}
        <button type="submit">Mettre à jour mon avatar</button>
        {{ form_end(form) }}
    </form>
{% endblock %}