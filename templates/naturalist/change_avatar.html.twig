{% extends "base.html.twig" %}

{% block body %}
    <h1>Change avatar</h1>

    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <button type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}